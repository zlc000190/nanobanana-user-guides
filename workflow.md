# Image-to-output workflow

A sensible test starts with one reference image and one text-only prompt. Compare prompt adherence, edge quality, text rendering, and subject consistency before testing animation or 3D conversion. [Nano Banana](https://nanobanana.page/) can be evaluated as an end-to-end workflow rather than only as an image generator.

Keep the original prompt and exports so later model changes can be compared fairly.
